<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acessoscartoes">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AcessosCartoes">
        <activity
            android:name=".ActivityListaLancamentos"
            android:label="@string/title_activity_lista_lancamentos"
            android:theme="@style/Theme.AcessosCartoes.NoActionBar"></activity>
        <activity android:name=".ActivityLancamentos" />
        <activity android:name=".ActivityLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AcessosCartoes.NoActionBar" />
        <activity
            android:name=".CadCartaoActivity"
            android:label="@string/title_activity_cad_cartao"
            android:theme="@style/Theme.AcessosCartoes.NoActionBar" />
    </application>

</manifest>